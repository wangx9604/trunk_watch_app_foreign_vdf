package com.xiaoxun.xun.utils;

public class HereUtils {
//    static final double EARTH_RADIUS = 6371009;
//
//    public static double computeHeading(GeoCoordinate from, GeoCoordinate to) {
//        // http://williams.best.vwh.net/avform.htm#Crs
//        double fromLat = Math.toRadians(from.getLatitude());
//        double fromLng = Math.toRadians(from.getLongitude());
//        double toLat = Math.toRadians(to.getLatitude());
//        double toLng = Math.toRadians(to.getLongitude());
//        double dLng = toLng - fromLng;
//        double heading = Math.atan2(
//                Math.sin(dLng) * Math.cos(toLat),
//                Math.cos(fromLat) * Math.sin(toLat) - Math.sin(fromLat) * Math.cos(toLat) * Math.cos(dLng));
//        return wrap(Math.toDegrees(heading), -180, 180);
//    }
//
//
//    static double wrap(double n, double min, double max) {
//        return (n >= min && n < max) ? n : (mod(n - min, max - min) + min);
//    }
//
//    static double mod(double x, double m) {
//        return ((x % m) + m) % m;
//    }
//
//    static double hav(double x) {
//        double sinHalf = sin(x * 0.5);
//        return sinHalf * sinHalf;
//    }
//
//    static double arcHav(double x) {
//        return 2 * asin(sqrt(x));
//    }
//
//    static double havDistance(double lat1, double lat2, double dLng) {
//        return hav(lat1 - lat2) + hav(dLng) * cos(lat1) * cos(lat2);
//    }
//
//    private static double distanceRadians(double lat1, double lng1, double lat2, double lng2) {
//        return arcHav(havDistance(lat1, lat2, lng1 - lng2));
//    }
//
//    /**
//     * Returns the angle between two LatLngs, in radians. This is the same as the distance
//     * on the unit sphere.
//     */
//    static double computeAngleBetween(GeoCoordinate from, GeoCoordinate to) {
//        return distanceRadians(Math.toRadians(from.getLatitude()), Math.toRadians(from.getLongitude()),
//                Math.toRadians(to.getLatitude()), Math.toRadians(to.getLongitude()));
//    }
//
//    /**
//     * Returns the distance between two LatLngs, in meters.
//     */
//    public static double computeDistanceBetween(GeoCoordinate from, GeoCoordinate to) {
//        return computeAngleBetween(from, to) * EARTH_RADIUS;
//    }
//    public static Bitmap rotateBimap(Context context, float degree, Bitmap srcBitmap) {
//        Matrix matrix = new Matrix();
//        matrix.reset();
//        matrix.setRotate(degree);
//        Bitmap bitmap = Bitmap.createBitmap(srcBitmap,0,0,srcBitmap.getWidth(),srcBitmap.getHeight()
//                ,matrix,true);
//        return bitmap;
//    }
//
//    public static boolean setIsolatedDiskCacheRootPath() {
//
//        return MapSettings.setIsolatedDiskCacheRootPath(
//                ImibabyApp.getMapHereDir().getPath(), "com.xiaoxun.xunoversea.MapService");
//    }
}
