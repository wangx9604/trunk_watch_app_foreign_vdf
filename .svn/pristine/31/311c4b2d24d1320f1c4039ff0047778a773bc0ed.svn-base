package com.xiaoxun.mapadapter.api;

import com.xiaoxun.mapadapter.bean.XunLatLng;

public interface XunMapUtils {

    float calculateArea(XunLatLng leftTopXunLatlng, XunLatLng rightBottomXunLatlng);

    float calculateArea(java.util.List<XunLatLng> xunPoints);

    float calculateLineDistance(XunLatLng startXunLatlng, XunLatLng endXunLatlng);
}
